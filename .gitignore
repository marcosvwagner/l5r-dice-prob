# Ignorar arquivos de sistema
.DS_Store        # Para macOS
Thumbs.db        # Para Windows

# Ignorar configurações específicas do VSCode
.vscode/          # Diretório de configurações do VSCode (ex: settings.json, etc.)

# Ignorar arquivos de ambiente (caso esteja utilizando)
.env             # Arquivos de configuração de ambiente (se necessário)

# Ignorar arquivos de build, caso use alguma ferramenta de bundling ou compilação (não necessário para HTML/CSS puros)
dist/            # Pasta de build, se usar ferramentas como Webpack, Parcel, etc.

# Ignorar arquivos temporários de editores de código
*.log            # Arquivos de log gerados por editores ou ferramentas
